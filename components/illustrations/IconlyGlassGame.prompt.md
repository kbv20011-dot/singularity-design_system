Иллюстративная стеклянная иконка «Game» из набора Iconly Glass. Роль: крупный акцент раздела или hero — прототипы и эксперименты. НЕ интерфейсная иконка: для интерфейса только `Icon` (lucide).

```jsx
<IconlyGlassGame style={{ color: 'var(--accent)' }} />
```

Родной размер из Figma (около 103×103), цвет из `currentColor`.
