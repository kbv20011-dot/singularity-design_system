Иллюстративная стеклянная иконка «Profile» из набора Iconly Glass. Роль: крупный акцент раздела или hero — клиент, команда, роль. НЕ интерфейсная иконка: для интерфейса только `Icon` (lucide).

```jsx
<IconlyGlassProfile style={{ color: 'var(--accent)' }} />
```

Родной размер из Figma (около 103×103), цвет из `currentColor`.
