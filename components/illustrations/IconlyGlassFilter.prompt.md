Иллюстративная стеклянная иконка «Filter» из набора Iconly Glass. Роль: крупный акцент раздела или hero — маршрутизация заявок, отбор. НЕ интерфейсная иконка: для интерфейса только `Icon` (lucide).

```jsx
<IconlyGlassFilter style={{ color: 'var(--accent)' }} />
```

Родной размер из Figma (около 103×103), цвет из `currentColor`.
