Полоса прогресса чтения, фиксирована сверху.

```jsx
<ScrollProgress />
```

Ставится один раз на длинную страницу (кейс, политика, лендинг сегмента) — рядом с `SiteHeader`, до контента. На коротких страницах не нужна: полоса, которая заполняется за один экран, только шумит.
